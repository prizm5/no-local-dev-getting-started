<div class="container">

  <table class="table table-striped table-bordered">
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
    </tr>

    <% @claims.each do |claim| %>
      <tr>
        <td><%= claim.id %></td>
      </tr>
    <% end %>
  </table>

</div>
